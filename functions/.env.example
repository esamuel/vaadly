# Firebase Functions v2 environment variables
# Copy to `.env` (not committed) and set your real values.

# Comma-separated list of UIDs who should receive platformAdmin=true in custom claims
PLATFORM_ADMIN_UIDS=your-admin-uid-1,your-admin-uid-2

# Shared secret for HTTPS seeding endpoint /seedMembership
SEED_ADMIN_KEY=replace-with-a-strong-random-string
